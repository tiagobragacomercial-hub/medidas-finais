# Portal do cliente

As rotas `/acesso` e `/p/{token}` consultam exclusivamente publicação imutável. O backend permite visualizar e downloads configurados e nega criar, alterar ou excluir. Códigos têm resposta genérica, limite de tentativas, expiração opcional e revogação.
