"use client";
import{useEffect,useMemo,useRef,useState}from"react";
import{useLiveQuery}from"dexie-react-hooks";
import{db,now,queue,uid}from"../database/local/db";
import type{Annotation,Client,Environment,Photo,Project}from"../types/models";
import{Camera,ClipboardList,Cloud,Download,FileText,FolderKanban,Home,ImagePlus,LayoutDashboard,Lock,Map,Plus,QrCode,Save,Settings,Trash2,Users}from"lucide-react";
import QRCode from"qrcode";import{jsPDF}from"jspdf";
type Section="dashboard"|"clients"|"projects"|"editor"|"floorplan"|"sync"|"portal";
const types=["Cozinha","Sala","Dormitório","Suíte","Banheiro","Lavabo","Lavanderia","Área gourmet","Corredor","Varanda","Escritório","Garagem","Comercial","Personalizado"];
export function MedidasApp(){const[section,setSection]=useState<Section>("dashboard"),[modal,setModal]=useState<null|"client"|"project"|"environment">(null),[toast,setToast]=useState("");const clients=useLiveQuery(()=>db.clients.toArray(),[])||[],projects=useLiveQuery(()=>db.projects.toArray(),[])||[],envs=useLiveQuery(()=>db.environments.toArray(),[])||[],photos=useLiveQuery(()=>db.photos.toArray(),[])||[],pending=useLiveQuery(()=>db.syncOperations.where("status").anyOf("pending","failed","conflict").count(),[])||0;useEffect(()=>{navigator.serviceWorker?.register("/sw.js").catch(()=>{})},[]);return <div className="app"><header className="topbar"><div className="brand"><div className="brandmark"><ClipboardList size={21}/></div><span>Medidas Finais</span></div><nav><button className="navbtn active">Operação</button><button className="navbtn">Publicações</button><button className="navbtn">Administração</button></nav><div className="sync"><span className="dot"/><span>{navigator.onLine?pending?`${pending} itens aguardando sincronização`:"Salvo neste dispositivo":"Sem internet — dados protegidos"}</span></div></header><div className="layout"><aside className="sidebar"><Side icon={<LayoutDashboard/>} label="Visão geral" active={section==="dashboard"} click={()=>setSection("dashboard")}/><div className="side-label">Levantamentos</div><Side icon={<Users/>} label="Clientes" active={section==="clients"} click={()=>setSection("clients")}/><Side icon={<FolderKanban/>} label="Projetos" active={section==="projects"} click={()=>setSection("projects")}/><Side icon={<ImagePlus/>} label="Editor de fotos" active={section==="editor"} click={()=>setSection("editor")}/><Side icon={<Map/>} label="Plantas baixas" active={section==="floorplan"} click={()=>setSection("floorplan")}/><div className="side-label">Entrega</div><Side icon={<Cloud/>} label="Sincronização" active={section==="sync"} click={()=>setSection("sync")}/><Side icon={<QrCode/>} label="Portal do cliente" active={section==="portal"} click={()=>setSection("portal")}/><Side icon={<Settings/>} label="Configurações"/></aside><main className="content">{section==="dashboard"&&<Dashboard clients={clients} projects={projects} envs={envs} photos={photos} pending={pending} go={setSection}/>} {section==="clients"&&<Clients clients={clients} add={()=>setModal("client")}/>} {section==="projects"&&<Projects projects={projects} clients={clients} add={()=>setModal("project")} addEnv={()=>setModal("environment")}/>} {section==="editor"&&<Editor envs={envs} photos={photos} notify={setToast}/>} {section==="floorplan"&&<FloorPlan/>} {section==="sync"&&<Sync pending={pending}/>} {section==="portal"&&<Portal projects={projects}/>}</main></div><div className="mobilebar"><button onClick={()=>setSection("dashboard")}><Home/><br/>Início</button><button onClick={()=>setSection("projects")}><FolderKanban/><br/>Projetos</button><button onClick={()=>setSection("editor")}><Camera/><br/>Medir</button><button onClick={()=>setSection("sync")}><Cloud/><br/>Sync</button></div>{modal&&<Modal type={modal} clients={clients} projects={projects} close={()=>setModal(null)} saved={()=>{setModal(null);setToast("Salvo neste dispositivo");setTimeout(()=>setToast(""),2200)}}/>}{toast&&<div className="toast">{toast}</div>}</div>}
function Side({icon,label,active,click}:{icon:React.ReactNode;label:string;active?:boolean;click?:()=>void}){return <button className={`sidebtn ${active?"active":""}`} onClick={click}><span style={{display:"inline-flex",verticalAlign:"middle",marginRight:10}}>{icon}</span>{label}</button>};
function Head({eye,title,sub,action}:{eye:string;title:string;sub:string;action?:React.ReactNode}){return <div className="pagehead"><div><div className="eyebrow">{eye}</div><h1>{title}</h1><p className="subtitle">{sub}</p></div>{action}</div>};
function Metric({label,value}:{label:string;value:number}){return <div className="card metric"><small>{label}</small><strong>{value}</strong></div>}
function Dashboard({clients,projects,envs,photos,pending,go}:{clients:Client[];projects:Project[];envs:Environment[];photos:Photo[];pending:number;go:(s:Section)=>void}){return <><Head eye="Painel operacional" title="Bom trabalho, Franciane" sub="Seus levantamentos ficam seguros neste dispositivo, mesmo sem internet." action={<button className="btn primary" onClick={()=>go("projects")}><Plus size={17}/>Novo levantamento</button>}/><div className="grid"><Metric label="Projetos ativos" value={projects.length}/><Metric label="Ambientes" value={envs.length}/><Metric label="Fotos salvas" value={photos.length}/><Metric label="Pendências de envio" value={pending}/><section className="card wide"><div className="cardhead"><h2>Levantamentos recentes</h2></div>{projects.length?projects.map(p=><div className="row" key={p.id}><div className="avatar">{p.name[0]}</div><div className="rowmain"><strong>{p.name}</strong><small>{clients.find(c=>c.id===p.clientId)?.name} · {p.unit}</small></div><span className="pill warn">Rascunho</span></div>):<div className="empty">Crie o primeiro cliente e levantamento para começar.</div>}</section><section className="card aside"><h2>Próximas ações</h2><div className="row"><div className="rowmain"><strong>Itens para sincronizar</strong></div><span className="pill">{pending}</span></div><div className="row"><div className="rowmain"><strong>Fotos sem parede</strong></div><span className="pill">{photos.length}</span></div></section></div></>}
function Clients({clients,add}:{clients:Client[];add:()=>void}){return <><Head eye="Cadastros" title="Clientes" sub="Informações internas ficam separadas do conteúdo publicado." action={<button className="btn primary" onClick={add}><Plus size={17}/>Novo cliente</button>}/><section className="card">{clients.length?clients.map(c=><div className="row" key={c.id}><div className="avatar">{c.name.slice(0,2).toUpperCase()}</div><div className="rowmain"><strong>{c.name}</strong><small>{c.phone||c.email||"Sem contato informado"}</small></div><span className={`pill ${c.status==="active"?"ok":""}`}>{c.status==="active"?"Ativo":"Arquivado"}</span><button className="btn" onClick={async()=>{await db.clients.update(c.id,{status:c.status==="active"?"archived":"active",updatedAt:now()});await queue("client",c.id,"update")}}>{c.status==="active"?"Arquivar":"Restaurar"}</button></div>):<div className="empty">Nenhum cliente cadastrado.</div>}</section></>}
function Projects({projects,clients,add,addEnv}:{projects:Project[];clients:Client[];add:()=>void;addEnv:()=>void}){return <><Head eye="Levantamentos" title="Projetos" sub="Organize ambientes, fotos e versões de cada entrega." action={<div style={{display:"flex",gap:8}}><button className="btn" onClick={addEnv}>+ Ambiente</button><button className="btn primary" onClick={add}>+ Novo projeto</button></div>}/><div className="grid">{projects.map(p=><article className="card" style={{gridColumn:"span 4"}} key={p.id}><div className="cardhead"><div className="avatar"><FolderKanban size={19}/></div><span className="pill warn">v{p.version}</span></div><h2>{p.name}</h2><p className="subtitle">{clients.find(c=>c.id===p.clientId)?.name}</p><div className="row"><small>{p.address||"Endereço não informado"}</small></div><button className="btn" style={{width:"100%",justifyContent:"center"}}>Abrir levantamento</button></article>)}{!projects.length&&<section className="card full empty">Crie um cliente e depois o primeiro projeto.</section>}</div></>}
function Modal({type,clients,projects,close,saved}:{type:"client"|"project"|"environment";clients:Client[];projects:Project[];close:()=>void;saved:()=>void}){const ref=useRef<HTMLFormElement>(null);async function submit(e:React.FormEvent){e.preventDefault();const f=new FormData(ref.current!),id=uid();if(type==="client")await db.clients.put({id,name:String(f.get("name")),phone:String(f.get("phone")||""),email:String(f.get("email")||""),address:String(f.get("address")||""),notes:String(f.get("notes")||""),status:"active",createdAt:now(),updatedAt:now()});else if(type==="project")await db.projects.put({id,clientId:String(f.get("clientId")),name:String(f.get("name")),address:String(f.get("address")||""),responsible:String(f.get("responsible")||""),unit:(f.get("unit")||"mm")as Project["unit"],status:"draft",version:1,createdAt:now(),updatedAt:now()});else await db.environments.put({id,projectId:String(f.get("projectId")),name:String(f.get("name")),type:String(f.get("envType")),status:"active"});await queue(type,id,"create");saved()}return <div className="modalback"><form ref={ref} className="modal" onSubmit={submit}><div className="cardhead"><h2>{type==="client"?"Novo cliente":type==="project"?"Novo projeto":"Novo ambiente"}</h2><button type="button" className="btn" onClick={close}>Fechar</button></div><div className="formgrid">{type==="project"&&<Field label="Cliente"><select name="clientId" required>{clients.map(c=><option value={c.id} key={c.id}>{c.name}</option>)}</select></Field>}{type==="environment"&&<><Field label="Projeto"><select name="projectId" required>{projects.map(p=><option value={p.id} key={p.id}>{p.name}</option>)}</select></Field><Field label="Tipo"><select name="envType">{types.map(t=><option key={t}>{t}</option>)}</select></Field></>}<Field label="Nome"><input name="name" required autoFocus/></Field>{type==="client"&&<><Field label="Telefone"><input name="phone"/></Field><Field label="E-mail"><input type="email" name="email"/></Field><Field label="Endereço" full><input name="address"/></Field><Field label="Observações internas" full><textarea name="notes" rows={3}/></Field></>}{type==="project"&&<><Field label="Endereço"><input name="address"/></Field><Field label="Responsável"><input name="responsible"/></Field><Field label="Unidade"><select name="unit"><option value="mm">Milímetros</option><option value="cm">Centímetros</option><option value="m">Metros</option></select></Field></>}</div><div className="actions"><button type="button" className="btn" onClick={close}>Cancelar</button><button className="btn primary"><Save size={16}/>Salvar no dispositivo</button></div></form></div>}
function Field({label,full,children}:{label:string;full?:boolean;children:React.ReactNode}){return <div className={`field ${full?"full":""}`}><label>{label}</label>{children}</div>}
function Editor({envs,photos,notify}:{envs:Environment[];photos:Photo[];notify:(s:string)=>void}){const[photoId,setPhotoId]=useState(photos[0]?.id||""),photo=photos.find(p=>p.id===photoId),anns=useLiveQuery(()=>photoId?db.annotations.where("photoId").equals(photoId).toArray():Promise.resolve([]),[photoId])||[],[tool,setTool]=useState("select"),[start,setStart]=useState<{x:number;y:number}|null>(null),[selected,setSelected]=useState("");const file=useRef<HTMLInputElement>(null),canvas=useRef<HTMLDivElement>(null);useEffect(()=>{if(!photoId&&photos[0])setPhotoId(photos[0].id)},[photos,photoId]);async function upload(e:React.ChangeEvent<HTMLInputElement>){const f=e.target.files?.[0];if(!f||!envs[0])return;const im=new Image(),url=URL.createObjectURL(f);im.onload=async()=>{const id=uid();await db.photos.put({id,environmentId:envs[0].id,name:f.name,blob:f,width:im.width,height:im.height,syncState:"local",createdAt:now()});await queue("photo",id,"upload");setPhotoId(id);URL.revokeObjectURL(url);notify("Foto original salva neste dispositivo")};im.src=url}async function draw(e:React.PointerEvent){if(tool!=="linear"||!photo)return;const r=canvas.current!.getBoundingClientRect(),p={x:(e.clientX-r.left)/r.width,y:(e.clientY-r.top)/r.height};if(!start)return setStart(p);const value=prompt("Informe a medida (somente número):","");if(value===null)return setStart(null);const id=uid();await db.annotations.put({id,photoId:photo.id,type:"linear",code:`M${String(anns.length+1).padStart(2,"0")}`,state:"protected",points:[start,p],value,textPosition:"above",description:"",layer:anns.length+1,version:1,updatedAt:now()});await queue("annotation",id,"create");setStart(null);setTool("select");notify("Medida salva e protegida")};async function edit(a:Annotation){await db.annotations.update(a.id,{state:"editing"});const value=prompt("Corrigir valor:",a.value);await db.annotations.update(a.id,{value:value??a.value,state:"protected",version:a.version+1,updatedAt:now()});await queue("annotation",a.id,"update")};const url=useMemo(()=>photo?URL.createObjectURL(photo.blob):"",[photo]);return <><Head eye="Editor vetorial" title="Medidas sobre a fotografia" sub="A imagem original nunca é alterada; as marcações ficam separadas." action={<><input ref={file} className="sr-only" type="file" accept="image/jpeg,image/png,image/heic" onChange={upload}/><button className="btn primary" onClick={()=>file.current?.click()}><ImagePlus size={17}/>Importar foto</button></>}/><div className="workspace"><div className="tools">{[["select","↖ Selecionar"],["linear","↔ Medida"],["l","⌞ Medida em L"],["angle","∠ Ângulo"],["point","⊙ Ponto técnico"],["text","T Texto"],["detail","◉ Foto detalhe"]].map(([id,label])=><button key={id} className={`tool ${tool===id?"active":""}`} onClick={()=>setTool(id)}>{label}</button>)}</div><div className="canvaswrap"><div ref={canvas} className="canvas" onPointerDown={draw}>{photo?<><img src={url} alt={photo.name}/>{anns.filter(a=>a.state!=="hidden").map(a=><Measure key={a.id} a={a} selected={a.id===selected} click={()=>setSelected(a.id)}/>)}</>:<div className="emptycanvas"><div><ImagePlus size={42}/><h3>Adicione uma fotografia</h3><p>Crie antes um projeto e ambiente.</p></div></div>}</div><div className="statusbar"><Lock size={12} style={{display:"inline"}}/> Salvo neste dispositivo</div></div><aside className="inspector"><div className="cardhead"><h2>Marcações</h2><span className="pill">{anns.length}</span></div>{anns.map(a=><div key={a.id} className={`annotation-item ${a.id===selected?"selected":""}`} onClick={()=>setSelected(a.id)}><strong>{a.code} · {a.value||"Pendente"}</strong><small style={{display:"block"}}>{a.state==="protected"?"Protegida":"Em edição"}</small>{a.id===selected&&<div style={{display:"flex",gap:5,marginTop:8}}><button className="btn" onClick={()=>edit(a)}><Lock size={13}/>Desbloquear</button><button className="btn danger" onClick={()=>db.annotations.update(a.id,{state:"hidden"})}><Trash2 size={13}/></button></div>}</div>)}</aside></div></>}
function Measure({a,selected,click}:{a:Annotation;selected:boolean;click:()=>void}){if(a.points.length<2)return null;const[p1,p2]=a.points,dx=(p2.x-p1.x)*100,dy=(p2.y-p1.y)*100,len=Math.hypot(dx,dy),ang=Math.atan2(dy,dx)*180/Math.PI;return <div className="measure" onClick={click} style={{left:`${p1.x*100}%`,top:`${p1.y*100}%`,width:`${len}%`,transform:`rotate(${ang}deg)`,height:selected?5:3,pointerEvents:"auto"}}><span>{a.value||"?"}</span></div>}
function FloorPlan(){return <><Head eye="Planta baixa" title="Desenho do ambiente" sub="Paredes e aberturas vetoriais editáveis, sem inventar dimensões."/><div className="grid"><section className="card wide"><div className="floorplan"><div className="wall" style={{left:"18%",top:"20%",width:"55%"}}/><div className="wall" style={{left:"73%",top:"20%",width:"50%",transform:"rotate(90deg)"}}/><div className="wall" style={{left:"18%",top:"72%",width:"55%"}}/><div className="wall" style={{left:"18%",top:"20%",width:"52%",transform:"rotate(90deg)"}}/><div className="door" style={{left:"18%",top:"52%"}}/></div></section><aside className="card aside"><h2>Elementos</h2><p className="subtitle">Parede · Porta · Janela · Vão · Coluna · Câmera</p><button className="btn">Retificar traços</button></aside></div></>}
function Sync({pending}:{pending:number}){return <><Head eye="Offline-first" title="Sincronização segura" sub="A cópia local só sai após confirmação válida do servidor."/><div className="grid"><Metric label="Na fila" value={pending}/><Metric label="Falhas" value={0}/><Metric label="Conflitos" value={0}/><Metric label="Dispositivos" value={1}/><section className="card full"><h2>Fila local protegida</h2><div className="progress" style={{marginTop:18}}><i/></div><p className="subtitle" style={{marginTop:12}}>Configure a API remota para enviar os itens. Nada será apagado localmente.</p></section></div></>}
function Portal({projects}:{projects:Project[]}){const[qr,setQr]=useState("");useEffect(()=>{QRCode.toDataURL(`${location.origin}/p/desenvolvimento-seguro`,{margin:2,width:180}).then(setQr)},[]);return <><Head eye="Entrega controlada" title="Portal do cliente" sub="Somente a versão publicada pode ser visualizada ou baixada."/><section className="portal-cover"><div className="eyebrow" style={{color:"#8ecbff"}}>Pasta digital</div><h1>{projects[0]?.name||"Apartamento Centro — Levantamento Completo"}</h1><p>Versão publicada 1 · acesso somente leitura</p></section><div className="portal-grid"><div className="portal-tile"><Map/><h2>Ambientes e plantas</h2><p className="subtitle">Navegação sem permissão de edição.</p></div><div className="portal-tile"><FileText/><h2>PDF técnico</h2><button className="btn" onClick={()=>pdf(projects[0])}><Download size={15}/>Gerar PDF local</button></div><div className="portal-tile">{qr&&<img src={qr} width="130" height="130" alt="QR Code de desenvolvimento"/>}<h2>Código: DEMO-4827</h2><p className="subtitle">Token revogável e sem dados pessoais.</p></div></div></>}
function pdf(p?:Project){const d=new jsPDF({orientation:"landscape",unit:"mm",format:"a4"});d.setFillColor(9,44,76);d.rect(0,0,297,55,"F");d.setTextColor(255);d.setFontSize(25);d.text("MEDIDAS FINAIS PARA PRODUCAO",18,31);d.setTextColor(30);d.setFontSize(15);d.text(p?.name||"Projeto demonstrativo",18,78);d.setFontSize(10);d.text(`Unidade: ${p?.unit||"mm"} | Versao: ${p?.version||1}`,18,89);d.text("Documento estruturado. Nenhuma medida foi estimada.",18,103);d.save("medidas-finais-para-producao.pdf")}
